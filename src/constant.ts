/**
 * @file Constants
 * @description Enum Constants
 * @author Peerbits
 */

/**
 * Token Type User Throughout Application
 */
export enum Token {
  INVITE = 'invite_token',
  ACCESS = 'access_token',
  RECOVERY = 'recovery_token'
}
